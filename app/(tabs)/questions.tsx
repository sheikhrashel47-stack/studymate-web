import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router } from "expo-router";
import { useMemo, useState } from "react";
import { FlatList, Pressable, StyleSheet, Text, TextInput, View } from "react-native";

import { AppHeader, Card, EmptyState, IconBadge, PrimaryButton, StudyScreen, colors } from "@/components/study/ui";
import { useStudy } from "@/lib/study/store";

const initials = (name: string) => name.trim().split(/\s+/).slice(0, 2).map((part) => part[0]).join("").toLocaleUpperCase() || "QB";

export default function QuestionsTab() {
  const { data, isReady } = useStudy();
  const [query, setQuery] = useState("");
  const visibleSubjects = useMemo(() => {
    const normalizedQuery = query.trim().toLocaleLowerCase();
    if (!normalizedQuery) return data.subjects;
    return data.subjects.filter((subject) => { const chapters = data.chapters.filter((chapter) => chapter.subjectId === subject.id); return [subject.name, ...chapters.map((chapter) => chapter.name)].join(" ").toLocaleLowerCase().includes(normalizedQuery); });
  }, [data.chapters, data.subjects, query]);

  if (!isReady) return <StudyScreen><AppHeader title="Question Bank" subtitle="Loading your local bank" /><View style={styles.center}><Text style={styles.loading}>Opening question bank…</Text></View></StudyScreen>;
  if (!data.questions.length) return <StudyScreen><AppHeader title="Question Bank" subtitle="সব Subject" right={<Pressable accessibilityRole="button" accessibilityLabel="Manage question bank" onPress={() => router.push("/questions/manage")} style={styles.headerIcon}><MaterialIcons name="settings" size={21} color={colors.ink} /></Pressable>} /><EmptyState icon="menu-book" title="No questions yet" detail="Import your first MCQs, choose a subject and chapter, then practise offline." action={<PrimaryButton label="Import questions" icon="file-upload" onPress={() => router.push("/questions/import")} />} /></StudyScreen>;
  return <StudyScreen><AppHeader title="Question Bank" subtitle="প্রথমে Subject নির্বাচন করুন" right={<View style={styles.headerActions}><Pressable accessibilityRole="button" accessibilityLabel="Search question bank" onPress={() => setQuery((value) => value ? "" : " ")} style={({ pressed }) => [styles.headerIcon, pressed && styles.pressed]}><MaterialIcons name="search" size={22} color={colors.ink} /></Pressable><Pressable accessibilityRole="button" accessibilityLabel="Manage question bank" onPress={() => router.push("/questions/manage")} style={({ pressed }) => [styles.headerIcon, pressed && styles.pressed]}><MaterialIcons name="settings" size={21} color={colors.ink} /></Pressable></View>} /><View style={styles.searchWrap}><MaterialIcons name="search" size={20} color={colors.muted} /><TextInput value={query.trim()} onChangeText={setQuery} placeholder="Search subjects or chapters" placeholderTextColor="#98A2B3" style={styles.searchInput} returnKeyType="search" /></View><FlatList data={visibleSubjects} numColumns={2} keyExtractor={(item) => item.id} contentContainerStyle={[styles.list, !visibleSubjects.length && { flexGrow: 1 }]} columnWrapperStyle={styles.columns} showsVerticalScrollIndicator={false} ListHeaderComponent={<Text style={styles.sectionLabel}>SELECT A SUBJECT</Text>} ListEmptyComponent={<EmptyState icon="search-off" title="No match found" detail="Try a different subject or chapter name." action={<PrimaryButton label="Clear search" variant="secondary" onPress={() => setQuery("")} />} />} renderItem={({ item, index }) => { const chapters = data.chapters.filter((chapter) => chapter.subjectId === item.id); const count = data.questions.filter((question) => question.subjectId === item.id).length; const tone = index % 3 === 0 ? { color: colors.blue, background: colors.softBlue } : index % 3 === 1 ? { color: "#7B4ED6", background: "#F0EAFE" } : { color: colors.success, background: "#E8F5EE" }; return <Pressable accessibilityRole="button" accessibilityLabel={`Open subject ${item.name}`} onPress={() => router.push({ pathname: "/questions/[subjectId]", params: { subjectId: item.id } })} style={({ pressed }) => [styles.cell, pressed && styles.pressed]}><Card style={styles.subjectCard}><View style={styles.subjectTop}><IconBadge icon="auto-stories" color={tone.color} background={tone.background} size={42} /><View style={styles.chevron}><MaterialIcons name="chevron-right" size={21} color={colors.muted} /></View></View><Text numberOfLines={1} style={styles.subjectName}>{item.name}</Text><Text style={styles.subjectMeta}>{chapters.length} topic{chapters.length === 1 ? "" : "s"} · {count} Q</Text></Card></Pressable>; }} ListFooterComponent={<View style={styles.footer}><PrimaryButton label="Import more questions" icon="file-upload" variant="secondary" onPress={() => router.push("/questions/import")} compact /></View>} /></StudyScreen>;
}

function TopicQuestionBank({ chapterName, questions, query, setQuery, total }: { chapterName: string; questions: { id: string; serial: number; prompt: string; options: Record<string, string>; explanation: string }[]; query: string; setQuery: (value: string) => void; total: number }) {
  return <StudyScreen><AppHeader title={chapterName} subtitle={`${total} questions`} right={<View style={styles.headerActions}><Pressable accessibilityRole="button" accessibilityLabel="Manage question bank" onPress={() => router.push("/questions/manage")} style={styles.headerIcon}><MaterialIcons name="settings" size={21} color={colors.ink} /></Pressable><Pressable accessibilityRole="button" accessibilityLabel="Import questions" onPress={() => router.push("/questions/import")} style={styles.headerIcon}><MaterialIcons name="add" size={22} color={colors.ink} /></Pressable></View>} /><View style={styles.topicSearch}><MaterialIcons name="search" size={20} color={colors.muted} /><TextInput value={query} onChangeText={setQuery} placeholder="Search questions, options, or explanation" placeholderTextColor="#98A2B3" style={styles.searchInput} returnKeyType="search" /></View><FlatList data={questions} keyExtractor={(item) => item.id} contentContainerStyle={[styles.questionList, !questions.length && { flexGrow: 1 }]} showsVerticalScrollIndicator={false} ListEmptyComponent={<EmptyState icon="search-off" title="No matching questions" detail="Try another word or clear the search." action={<PrimaryButton label="Clear search" variant="secondary" onPress={() => setQuery("")} />} />} renderItem={({ item }) => <Pressable accessibilityRole="button" accessibilityLabel={`Open question ${item.serial}`} onPress={() => router.push({ pathname: "/questions/detail/[questionId]", params: { questionId: item.id } })} style={({ pressed }) => [pressed && styles.pressed]}><Card style={styles.questionCard}><View style={styles.cardTop}><View style={styles.typeBadge}><Text style={styles.typeText}>MCQ</Text></View><View style={styles.cardTopRight}><Text style={styles.serial}>Q{item.serial}</Text><MaterialIcons name="chevron-right" size={22} color={colors.muted} /></View></View><Text style={styles.breadcrumb}>{chapterName} · Practice</Text><Text style={styles.question}>{item.prompt}</Text><View style={styles.options}>{Object.entries(item.options).filter(([, value]) => Boolean(value)).map(([key, value]) => <View key={key} style={styles.option}><View style={styles.optionKey}><Text style={styles.optionKeyText}>{key}</Text></View><Text style={styles.optionText}>{value}</Text></View>)}</View><Text style={styles.questionMeta}>{Object.keys(item.options).length} options · Answer saved · {item.explanation === "Explanation unavailable" ? "No explanation" : "Explanation included"}</Text></Card></Pressable>} /></StudyScreen>;
}

const styles = StyleSheet.create({
  center: { flex: 1, alignItems: "center", justifyContent: "center" }, loading: { color: colors.muted, fontSize: 14 }, headerActions: { flexDirection: "row", gap: 7 }, headerIcon: { width: 40, height: 40, borderRadius: 13, alignItems: "center", justifyContent: "center", backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.emeraldBorder }, pressed: { opacity: 0.7, transform: [{ scale: 0.98 }] }, searchWrap: { marginHorizontal: 18, marginBottom: 9, minHeight: 46, flexDirection: "row", alignItems: "center", gap: 9, paddingHorizontal: 13, borderRadius: 14, borderWidth: 1, borderColor: colors.emeraldBorder, backgroundColor: colors.glass }, topicSearch: { marginHorizontal: 10, marginBottom: 7, minHeight: 46, flexDirection: "row", alignItems: "center", gap: 9, paddingHorizontal: 13, borderRadius: 14, borderWidth: 1, borderColor: colors.emeraldBorder, backgroundColor: colors.glass }, searchInput: { flex: 1, color: colors.ink, fontSize: 15, paddingVertical: 10 }, list: { paddingHorizontal: 18, paddingBottom: 28, gap: 9 }, columns: { gap: 9 }, sectionLabel: { color: colors.muted, fontSize: 11, fontWeight: "900", letterSpacing: 1.2, marginBottom: 1 }, cell: { flex: 1, minWidth: 0 }, subjectCard: { minHeight: 126, padding: 13, justifyContent: "space-between", borderRadius: 17 }, subjectTop: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, chevron: { width: 26, height: 26, alignItems: "center", justifyContent: "center" }, subjectName: { color: colors.ink, fontSize: 16, fontWeight: "800", marginTop: 9 }, subjectMeta: { color: colors.muted, fontSize: 12, marginTop: 3 }, footer: { paddingTop: 2, paddingBottom: 6 }, questionList: { paddingHorizontal: 10, paddingBottom: 28, gap: 9 }, questionCard: { padding: 14, borderRadius: 18, borderTopWidth: 0, shadowOpacity: 0.04, elevation: 1 }, cardTop: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, cardTopRight: { flexDirection: "row", alignItems: "center", gap: 6 }, typeBadge: { paddingHorizontal: 11, paddingVertical: 7, borderRadius: 11, borderWidth: 1, borderColor: colors.emeraldBorder, backgroundColor: colors.softBlue }, typeText: { color: colors.blueDark, fontSize: 12, fontWeight: "900", letterSpacing: 1 }, serial: { color: colors.muted, fontSize: 12, fontWeight: "900" }, breadcrumb: { color: colors.muted, fontSize: 12, marginTop: 12 }, question: { color: colors.ink, fontSize: 18, lineHeight: 26, fontWeight: "900", marginTop: 10 }, options: { gap: 7, marginTop: 16 }, option: { minHeight: 47, flexDirection: "row", alignItems: "center", gap: 10, borderRadius: 12, borderWidth: 1, borderColor: colors.emeraldBorder, backgroundColor: colors.glass, paddingHorizontal: 10, paddingVertical: 7 }, optionKey: { width: 29, height: 29, borderRadius: 10, alignItems: "center", justifyContent: "center", backgroundColor: colors.softBlue }, optionKeyText: { color: colors.blueDark, fontSize: 12, fontWeight: "900" }, optionText: { flex: 1, color: colors.ink, fontSize: 14, lineHeight: 19 }, questionMeta: { color: colors.muted, fontSize: 11, marginTop: 12 },
});
